let price = 20  // Bitcoin price
let x = 30      // high value
let y = 10      // low value

if (price > x) {
  console.log('Sell !')
} else if (price < y) {
  console.log('Buy !')
} else {
  console.log('Hold')
}